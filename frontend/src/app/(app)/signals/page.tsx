import { PhaseStub } from "@/components/ui/phase-stub";

export default function SignalsPage() {
  return (
    <PhaseStub
      phase="Phase D — Coming Soon"
      title="ML Signals"
      description="A sentiment-to-price signal model plus the entity relationship graph extracted from the news."
      color="#f59e0b"
    />
  );
}
