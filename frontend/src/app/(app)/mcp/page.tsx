import { PhaseStub } from "@/components/ui/phase-stub";

export default function McpPage() {
  return (
    <PhaseStub
      phase="Phase C — Coming Soon"
      title="MCP Server"
      description="Expose QuantMind as tools to Claude Desktop / Cursor via the Model Context Protocol."
      color="#a855f7"
    />
  );
}
